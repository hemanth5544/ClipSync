# Railway Configuration for Go Backend (no Docker)
# In Railway Dashboard: Settings → Source → Root Directory = "apps/backend"

[build]
builder = "RAILPACK"
buildCommand = "go build -o server ./cmd/server"

[deploy]
startCommand = "./server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
